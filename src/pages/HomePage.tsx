import { Box, Typography } from "@mui/material";
import React from "react";

function HomePage() {
  return (
    <Box display={"flex"} justifyContent={"center"} alignItems={"center"}>
      <Typography variant="h4" color={"red"}>HomePage</Typography>
    </Box>
  );
}

export default HomePage;
